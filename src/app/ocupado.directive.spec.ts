import { OcupadoDirective } from './ocupado.directive';

describe('OcupadoDirective', () => {
  it('should create an instance', () => {
    const directive = new OcupadoDirective();
    expect(directive).toBeTruthy();
  });
});
